@model EntityDetailsViewModel<Category>

@{
    ViewBag.Title = "Categories";
}

@Html.Partial("_JumboHeader")

<div class="container">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Entity.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Entity.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Entity.Type)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Entity.Type)
        </dd>

    </dl>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <a id="edit" class="btn btn-default disabled" href="@Url.Action( "Edit", new { id = Model.Entity.Id } )"><i cla><i class="glyphicon glyphicon-pencil"></i>&nbsp;Edit</a>
        </div>
    </div>


    <hr />
    <p>
        <a class="btn btn-default" href="@Url.Action("Index")"><i class="glyphicon glyphicon-arrow-left"></i>&nbsp;Back to List</a>
    </p>

    <hr />

    <p>
        @Html.Partial("_CategoryTransactionsList", Model.Transactions)
    </p>
</div>

